{
  "main": "rest/index.js",
  "scripts": {
    "start": "node rest/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix app && npm run build --prefix app"
  },
  "dependencies": {
    "@angular/cli": "^17.1.0"
  }
}
